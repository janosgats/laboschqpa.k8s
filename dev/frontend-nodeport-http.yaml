apiVersion: v1
kind: Service
metadata:
  name: frontend-nodeport-http
spec:
  type: NodePort
  selector:
    component: frontend
  ports:
  - name: http-frontend
    port: 3000
    targetPort: 4200
    nodePort: 30300