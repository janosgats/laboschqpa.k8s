apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  labels:
    component: server
spec:
  replicas: 1
  selector:
    matchLabels: 
      component: server
  template:
    metadata:
      labels:
        component: server
    spec:
      containers:
        - name: server
          image: gjani/laboschqpa-server
          ports:
            - containerPort: 8080
          volumeMounts:
          - mountPath: "/app/secret/"
            name: secrets
      volumes:
        - name: secrets
          secret:
            secretName: laboschqpa-server-secrets