apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filehost-pvc-localdiskfiles
spec:
  storageClassName: manual
  volumeName: pv-volume-filehost-localdiskfiles
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Mi