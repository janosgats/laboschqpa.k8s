apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    component: frontend
spec:
  replicas: 1
  selector:
    matchLabels: 
      component: frontend
  template:
    metadata:
      labels:
        component: frontend
    spec:
      containers:
        - name: frontend
          image: szalkaikrisztian/laboschqpa-frontend
          ports:
            - containerPort: 3000
            - containerPort: 30015
#          volumeMounts:
#            - mountPath: "/app/node_modules/"
#              name: node-modules
#      volumes:
#        - name: node-modules
#          persistentVolumeClaim:
#            claimName: frontend-pvc-nodemodules-for-dev